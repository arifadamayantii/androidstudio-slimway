<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LoginForm" parent="Theme.Material3.Light.NoActionBar">
        <item name="elevationOverlayEnabled">false</item>
        <item name="android:windowLightStatusBar">true</item> <!-- icon status bar hitam -->
        <item name="android:statusBarColor">@android:color/white</item> <!-- background putih -->
    </style>

    <style name="Theme.LoginForm" parent="Base.Theme.LoginForm" />


</resources>